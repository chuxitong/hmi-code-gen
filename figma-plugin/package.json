{
  "name": "figma-hmi-plugin",
  "version": "0.1.0",
  "description": "Figma plugin that turns industrial HMI mockups into HTML/CSS via a local VLM service.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "^5.4.0"
  }
}
